// Type Content here...
#include <stdio.h>
int main()
{
	int n,i,num;
	int posum=0, negsum=0;
	scanf ("%d",&n);
	for (i=0;i<n;i++)
		{
			scanf("%d",&num);
			if(num>0)
				posum+=num;
			else if(num<0)
				negsum+=num;
		}
	printf("%d\n",posum);
	printf("%d\n",negsum);
	return 0;
}
